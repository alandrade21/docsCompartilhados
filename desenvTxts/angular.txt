- Instalar node JS

Nodejs deve estar instalado. Veja arquivo específico.

- Instala o angular cli

$ sudo npm install -g @angular/cli

- Se der loop infinito, instala e tenta de novo

$ sudo npm install -g node-gyp

- testa

$ ng -v

- Instala o typescript

veja arquivo específico.
